项目已经初具雏形了，
准备把它上传到github。
捣鼓了一上午，
不知道怎么才能把仓库本身作为一个项目而不是把一个项目放在仓库里面，
没办法，
只能先手动把项目的配置文件上传了，
这样eclipse就可以将它识别为一个项目了。

import过程中出现一个错误，
说这个项目已经存在一个.project了，
在本地把这个.project删了，
成功import。
回头再把eclipse新生成的.project换成原来的，
右键refresh，
问题解决(次数+1)

饿死我了，
连打字也有种有气无力的赶紧，
吃饭吃饭。
